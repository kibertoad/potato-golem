{
  "name": "@potato-golem/deadlines-in-hell",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:ts": "tsc",
    "build:production:full": "del-cli dist && del-cli ../../.parcel-cache --force && npm run build:deps && npm run build:production",
    "build:production": "parcel build assets/html/index.html ./src/**/*.ts  --config ./parcel/.parcel.prod",
    "build:deps": "cd ../../packages && cd core && npm run build && cd .. && cd ui && npm run build && cd ../../games && cd deadlines-in-hell && npm run build:ts",
    "build:dev-full": "npm run build:deps && parcel assets/html/index.html ./src/**/*.ts",
    "build:dev": "parcel assets/html/index.html ./src/**/*.ts",
    "lint": "biome check index.ts src",
    "lint:fix": "biome check --write src",
    "test": "vitest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kibertoad/potato-golem.git"
  },
  "author": "kibertoad",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kibertoad/potato-golem/issues"
  },
  "homepage": "https://github.com/kibertoad/potato-golem#readme",
  "dependencies": {
    "@potato-golem/core": "latest",
    "@potato-golem/ui": "latest",
    "awilix": "^10.0.2",
    "generate-human-names": "^1.2.2",
    "phaser": "^3.80.1",
    "phaser3-rex-plugins": "^1.80.5",
    "validation-utils": "^10.0.0"
  },
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.12.0",
    "@biomejs/biome": "^1.7.3",
    "del-cli": "^5.1.0",
    "vitest": "^1.6.0",
    "parcel": "^2.12.0",
    "parcel-reporter-clean-dist": "^1.0.4",
    "parcel-reporter-static-files-copy": "^1.5.3"
  }
}
