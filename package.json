{
  "name": "@potato-golem/parent",
  "version": "1.0.0",
  "scripts": {
    "format": "npm run format --workspaces",
    "build:deadlines-in-hell": "cd games/deadlines-in-hell && npm run build:production",
    "build:shadow-agents": "cd games/shadow-agents && npm run build:production"
  },
  "peerDependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "phaser": "^3.80.1"
  },
  "workspaces": [
    "packages/*",
    "games/*"
  ]
}
